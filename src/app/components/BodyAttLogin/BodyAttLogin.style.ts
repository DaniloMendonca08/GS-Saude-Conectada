import { colors } from "@/styles/colors";
import styled from "styled-components";

export const ContainerAttLogin = styled.div`
    text-align: center;
    background-color: ${colors.primary.lightBlue};
    padding: 4vh 0;
    height: 140vh;
`